# Retail ETL example

A minimal productive batch flow: CSV source → filter complete orders → select columns → materialized view. Create it with:

```bash
sdpstudio project create retail-demo --from-example retail-etl
```
