# ADR-002: Visual YAML plus generated source

**Decision:** The visual model and generated Python/SQL are durable representations, with the model as the canonical semantic input.

**Consequences:** Generation is deterministic and source maps preserve navigation.
